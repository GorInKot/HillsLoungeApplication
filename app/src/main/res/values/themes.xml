<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.HillsLoungeApplication" parent="Theme.Material3.DayNight.NoActionBar">
<!--        <item name="android:windowBackground">@drawable/splash_background</item>-->
        <!-- Фон приложения -->
        <item name="background">@color/background_variant</item>

        <!-- Прочие настройки вашего приложения -->
    </style>

    <!-- Стиль кнопок для светлой темы -->
    <style name="ButtonStyle.HookahBar.Light" parent="Widget.MaterialComponents.Button">
        <item name="backgroundTint">@color/luxury_light_primary</item>
        <item name="android:textColor">@color/luxury_light_text_primary</item>
    </style>

    <style name="ButtonStyle.HookahBar.Light_Variant" parent="Widget.MaterialComponents.Button">
        <item name="backgroundTint">@color/luxury_light_primary</item>
        <item name="android:textColor">@color/textColor</item>
    </style>

    <style name="MyTextViewStyle">
        <item name="android:textSize">18sp</item>
    </style>

    <!-- Стиль для заставки (Splash Screen) -->
    <style name="Theme.HillsLoungeApplication.Splash" parent="Theme.MaterialComponents.Light.NoActionBar"></style>

</resources>
